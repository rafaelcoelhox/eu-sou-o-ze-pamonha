services:
  canjica: &api
    image: ${RINHA_IMAGE:-ghcr.io/rafaelcoelhox/eu-sou-o-ze-pamonha:rc1-v7}
    command: ["/usr/local/bin/canjica"]
    environment:
      RINHA_SOCK: /run/sock/canjica.sock
      RINHA_INDEX_PATH: /app/data/index.ivf
    volumes:
      - sock:/run/sock
    networks:
      - rinha
    deploy:
      resources:
        limits:
          memory: "160MB"
    cpu_period: 10000
    cpu_quota: 4500
    mem_limit: 160m
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    security_opt:
      - seccomp:unconfined
  pamonha:
    <<: *api
    command: ["/usr/local/bin/pamonha"]
    environment:
      RINHA_SOCK: /run/sock/pamonha.sock
      RINHA_INDEX_PATH: /app/data/index.ivf
  lb:
    image: ${RINHA_IMAGE:-ghcr.io/rafaelcoelhox/eu-sou-o-ze-pamonha:rc1-v7}
    command: ["/usr/local/bin/carro-da-pamonha"]
    depends_on:
      - canjica
      - pamonha
    environment:
      RINHA_LB_PORT: "9999"
      RINHA_UPSTREAMS: /run/sock/canjica.sock,/run/sock/pamonha.sock
    ports:
      - "9999:9999"
    volumes:
      - sock:/run/sock
    networks:
      - rinha
    deploy:
      resources:
        limits:
          memory: "30MB"
    cpu_period: 10000
    cpu_quota: 1000
    mem_limit: 30m
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    security_opt:
      - seccomp:unconfined
networks:
  rinha:
    driver: bridge
volumes:
  sock:
